{
  "$schema": "https://raw.githubusercontent.com/anthropics/mcpb/main/schemas/mcpb-manifest-v0.4.schema.json",
  "manifest_version": "0.4",
  "name": "cozi-mcp",
  "display_name": "Cozi Family Organizer",
  "version": "2.1.1",
  "description": "Manage your Cozi lists, calendar, and family members from your AI assistant.",
  "long_description": "Unofficial MCP server for the Cozi Family Organizer. Lets your AI assistant read and update your shared shopping lists, to-do lists, and family calendar. Your Cozi credentials are stored in your OS keychain by Claude Desktop and never leave your machine — the author of this extension never sees your data.",
  "author": {
    "name": "Matthew Jucius",
    "email": "matthew@jucius.com",
    "url": "https://github.com/mjucius"
  },
  "homepage": "https://github.com/mjucius/cozi_mcp",
  "repository": {
    "type": "git",
    "url": "https://github.com/mjucius/cozi_mcp"
  },
  "license": "MIT",
  "icon": "assets/cozi_mcp_logo.png",
  "keywords": ["cozi", "family", "calendar", "lists", "todo", "shopping"],
  "server": {
    "type": "node",
    "entry_point": "dist/bin.js",
    "mcp_config": {
      "command": "node",
      "args": ["${__dirname}/dist/bin.js"],
      "env": {
        "COZI_USERNAME": "${user_config.username}",
        "COZI_PASSWORD": "${user_config.password}",
        "COZI_READ_ONLY": "${user_config.read_only}"
      }
    }
  },
  "user_config": {
    "username": {
      "type": "string",
      "title": "Cozi username (email)",
      "description": "Your Cozi account email address.",
      "required": true
    },
    "password": {
      "type": "string",
      "title": "Cozi password",
      "description": "Your Cozi account password. Stored securely in your OS keychain.",
      "required": true,
      "sensitive": true
    },
    "read_only": {
      "type": "boolean",
      "title": "Read-only mode",
      "description": "Only expose read tools and hide tools that create, update, or delete Cozi data.",
      "default": false,
      "required": false
    }
  },
  "tools_generated": true,
  "compatibility": {
    "claude_desktop": ">=0.10.0",
    "platforms": ["darwin", "win32", "linux"],
    "runtimes": {
      "node": ">=20.0.0"
    }
  }
}
